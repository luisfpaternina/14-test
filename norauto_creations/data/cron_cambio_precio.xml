<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="ir_cron_notificar_fin_precio" model="ir.cron">
            <field name="name">Fin de precio de venta</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).strftime('%Y-%m-%d 04:00:00')" />
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field eval="False" name="doall"/>
            <field name="priority" eval="0" />
            <field name="model_id" ref="norauto_creations.model_product_template"/>
            <field name="state">code</field>
            <field name="code">model.actualizar_precio_actual()</field>
        </record>

    </data>
</odoo>
