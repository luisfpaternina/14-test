<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="product_template_only_form_view_etiquetas" model="ir.ui.view">
        <field name="name">product product etiquetas</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            <field name="uom_po_id" position="after">
                <field name="unimes"/>
                <field name="nbrunimes"/>
                <field name="precio_uni_med"/>
                <field name="formated_date" readonly="1"/>
            </field>
        </field>
    </record>

</odoo>
